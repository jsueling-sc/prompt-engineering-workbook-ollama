[project]
name = "prompt-engineering-workbook"
version = "0.1.0"
description = "A hands-on prompt-engineering tutorial running against a local Ollama model."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ollama>=0.6",
    "jupyterlab>=4.4",
]

[tool.uv]
package = false